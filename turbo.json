{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "ui#build": {
      "dependsOn": ["^build", "@repo/db#build","@repo/core#build"],
      "outputs": ["./.next/**", "./.next/cache/**"],
      "outputLogs": "full"
    },
    "api#build": {
      "dependsOn": ["^build","@repo/db#build","@repo/core#build","@repo/tsconfig#build"],
      "outputs": ["./build/**"],
      "outputLogs": "full"
    },
    "core#build": {
      "dependsOn": ["^build","@repo/db#build","@repo/tsconfig#build"],
      "outputs": ["./build/**"],
      "outputLogs": "full"
    },
    "@repo/db#build": {
      "dependsOn": ["^build"],
      "outputs": ["./build/**"],
      "outputLogs": "full"
    },
    "@repo/core#build": {
      "dependsOn": ["^build"],
      "outputs": ["./build/**"],
      "outputLogs": "full"
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "@repo/tsconfig#build":{ },
    "dev": {
      "persistent": true,
      "cache": true
    }
  }
}