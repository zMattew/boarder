{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "ui#build": {
      "dependsOn": ["^build"],
      "outputs": ["./.next/**", "./.next/cache/**"],
      "outputLogs": "full"
    },
    "core#build": {
      "dependsOn": ["^build"],
      "outputs": ["../build/**"],
      "outputLogs": "full"
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": true
    }
  }
}